---
- name: Update the aws_region value in appconfig.yaml
  replace:
    path: "{{ component_path }}/config/appconfig.yaml"
    regexp: 'region_name:\s*"[^"]*"'
    replace: 'region_name: "{{ region_name }}"'